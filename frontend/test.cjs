const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('pageerror', err => console.log('PAGE ERROR:', err.message)); page.on('console', msg => console.log('CONSOLE:', msg.text())); await page.goto('http://localhost:3000/briefing/706eb324-8717-4f4d-97ac-5500f1687a07', {waitUntil: 'networkidle0'}).catch(e => console.log('Goto error:', e)); await new Promise(r => setTimeout(r, 2000)); await browser.close(); })();
